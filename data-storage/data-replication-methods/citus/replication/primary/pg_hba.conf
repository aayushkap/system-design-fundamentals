# replication/primary/init.sql
# Allow replication connections from any IP using md5
host    replication     replicator      0.0.0.0/0               md5

# Keep your normal local/auth settings below:
local   all             all                                     peer
host    all             all             0.0.0.0/0               md5
